# Jumpwhere Internship Assignments

This repository contains the Python solutions and interview-practice files created during the internship assignments.

## Structure

- `Assignment-1/`
  - `Part-A/index.html`
  - `Part-B/index.html`
- `Assignment-2/`
  - `2.1/` – optimized Python solutions with a short README note
  - `2.2/` – popular coding interview problem solutions

## Notes

- All Python scripts are standalone and can be run directly.
- The `Assignment-2/2.2` folder has been validated by running each `.py` file.

## Next step

Add your GitHub remote and push:

```bash
git remote add origin <your-github-repo-url>
git branch -M main
git push -u origin main
```
